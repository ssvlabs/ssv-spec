package commit

import "github.com/bloxapp/ssv-spec/qbft/spectest/tests"

// WrongMsgType tests a commit msg received with the wrong msg type (not a commit msg type)
func WrongMsgType() *tests.MsgProcessingSpecTest {
	panic("implement")
}
